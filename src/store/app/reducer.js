const initialState = {
  language: ''
};

const app = (state = initialState, action) => {
  // let _state;

  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default app;
